from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='An AI trained to generate the contents of an arbitrary image region conditioned on its surroundings. ',
    author='Guillem96',
    license='MIT',
)
